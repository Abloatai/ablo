---
title: "Changelog"
description: "Product updates and releases for Ablo"
---

<Update label="June 24, 2026" description="v0.16.2">
## Name the shared-schema binding around the project

`mintUserSessionKey` replaces the two flat options from 0.16.0
(`schemaOwnerOrgId` + `schemaProjectId`) with one project-centric option —
`schemaProject: { organizationId, projectId }` — naming "the project that owns
the schema" as a single concept. The wire format is unchanged.

```ts
mintUserSessionKey({
  organizationId,                                // data org
  schemaProject: { organizationId, projectId },  // the project that owns the schema
  // ...
});
```
</Update>

<Update label="June 24, 2026" description="v0.16.1">
## Fix `ablo login`

`ablo login` now completes reliably. The device-authorization endpoints resolve
against the identity server (override with `ABLO_AUTH_URL`) and the approval page
against the dashboard (new override `ABLO_DASHBOARD_URL`), fixing a "Couldn't
start login" error where both were assumed to share one origin.
</Update>

<Update label="June 24, 2026" description="v0.16.0">
## Axis 3 — declare write-conflict behaviour in the schema

A model can now state what happens when a commit collides with a foreign claim
or a stale snapshot — per committer kind (`user` / `agent` / `system`) — right
next to its fields, using the same `overwrite | reject | notify` vocabulary as
the `onStale` write guard. A third axis, orthogonal to `policy` (read access)
and `groups` (delta routing).

```ts
import { coordination, humansOverwrite, agentsReject } from '@abloatai/ablo/schema';

// "a human's edit always wins (never blocked); an agent yields"
conflict: coordination(humansOverwrite(), agentsReject());
// → { user: 'overwrite', agent: 'reject' }
```

It is pure, serializable data — the generic engine interprets it at the commit
chokepoint. New `@abloatai/ablo/schema` exports: `coordination`, the
`humans*` / `agents*` / `system*` disposition helpers, and the `ConflictRule`
type; `ConflictAxis` (also `Ablo.Conflict.Axis`) and `interpretConflictAxis`
are exported for custom policies. An omitted committer kind falls through to the
engine default (reject; honor `onStale: 'notify'`) — fully additive.

### Added

- **First-party shared schema for ephemeral keys.** `mintUserSessionKey` accepts
  `schemaProjectId` + `schemaOwnerOrgId`, binding the minted `ek_` so **schema**
  resolves from a shared owner-org/project (one schema serves all of an
  integrator's end-user orgs) while **data** stays scoped to `organizationId`.
  Requires the `sk_` to carry `ephemeral:mint-any-org`; omit both for the
  existing per-org behaviour.
</Update>

<Update label="June 23, 2026" description="v0.15.1">
## Loud 0-row writes

A commit now reports the ids of any UPDATE/DELETE that matched zero rows on
`CommitReceipt.missingIds`, and the new exported `AbloNotFoundError` lets typed
write wrappers throw instead of silently treating a missed write as success.
Additive and back-compatible — the field is omitted when nothing missed.
</Update>

<Update label="June 22, 2026" description="v0.15.0">
## Notify-instead-of-abort: non-coercive conflict handling + read-set

On a stale-context conflict the engine now surfaces the current state and lets
the actor — agent or human — resolve it, instead of forcing an outcome.

### Added

- **`onStale: 'notify'`** holds the conflicting write (not applied) and returns
  a `StaleNotification` carrying the field's current value, so the actor
  reconciles and re-commits rather than losing work. Delivered on
  `CommitReceipt.notifications` and the new `conflict:notified` SDK event.
- **Read-set (`reads[]`)** — declare what you looked at, not just what you write.
  A moved premise fires that entry's `onStale` over the whole batch. Row
  (`{ model, id, readAt, fields? }`) and group (`{ group, readAt }`)
  granularities. New `ReadDependency` type on `ablo.commits.create`.
- **Data Source reverse-channel connector** — `createSourceConnector({ apiKey, handler })`
  dials out to the engine over one outbound WebSocket (`ablo.source.v1`) instead
  of exposing an inbound HTTP endpoint, for private/VPC stores. The
  Standard-Webhooks HMAC is signed above the transport, so the trust model is
  unchanged.

### Breaking

- **`onStale` values are Stripe-aligned.** `'force'` → `'overwrite'`;
  `'flag'` / `'merge'` → `'notify'` (both removed). The set is now
  `'reject' | 'overwrite' | 'notify'`.
</Update>

<Update label="June 21, 2026" description="v0.14.0">
## Claim API consistency + coordination docs

`useWatch` (scoped presence + read-interest) and `usePeers` (read-only presence)
are now documented. `HttpClaimApi` is a mechanically derived async projection of
the reactive `ClaimApi`, so the two transports can't drift. The claim read verb
is unified to `state` across every layer. No behaviour change.
</Update>

<Update label="June 17, 2026" description="v0.13.0">
## Schema authoring: two orthogonal axes — `policy` and `groups`

### Breaking

- The flat model options are replaced by two namespaced ones:
  - **`policy`** — row access / tenant isolation. A discriminated union on `by`
    replaces `orgScoped` / `scopedVia` / `orgColumn`: `{ by: 'column' }`
    (default) or `{ by: 'parent', fk, parent }` to inherit tenancy through a
    foreign key. `TenancyInput` → `PolicyInput`.
  - **`groups: { root, grants, roles }`** — which delta channels a row fans into,
    replacing flat `scope` / `grants` / `entityRoles`.
- CLI: `config.json` stores per-project profile key pairs; older flat layouts
  fold into the active profile automatically.
</Update>

<Update label="June 16, 2026" description="v0.12.0">
## Canonicalize the claim API to one vocabulary

### Breaking

- Claim phase field `action` → `reason` on every claim surface (the wire is
  unchanged — still `action`, healed on read; no server redeploy).
- Claim contention flag `wait` → `queue` everywhere.
- React hook `useParticipant` → `useWatch`.
- `ClaimDeclaration.ttlSeconds` is now a `number` (was a `Duration`).
</Update>

<Update label="June 16, 2026" description="v0.11.2">
## Undo correctness + one-correct-way consolidation

Fixes stream-recorded undo capturing the wrong "before" value on a quick second
edit, and closes the create/update undo asymmetry. Both undo paths now share one
before-image implementation.

### Breaking

- Credentials collapse to a single `apiKey` (string or `() => Promise<string | null>`);
  removed `getToken` / `authEndpoint` / public `authToken`.
- `ablo.<model>.watch(ids, { ttl })` replaces `ablo.participants.join({ scope })`.
- Read claim-gating is `ifClaimed: 'return' | 'fail'` (removed `'wait'`).
- `createAbloHttpClient` is no longer a public export — use `Ablo({ transport: 'http' })`.
- Read-option types renamed: `ServerReadOptions` / `LocalReadOptions`.
</Update>

<Update label="June 15, 2026" description="v0.11.1">
## DX hardening from a real onboarding session

- `databaseUrl` is now an explicit, server-only option — `Ablo(...)` no longer
  auto-reads `process.env.DATABASE_URL` (a stray one no longer flips the client
  into connection-string mode).
- Claims/presence are observable from any client (including Node agents):
  reading a row enters its sync group, claiming pins it. **Requires a
  coordinated `sync-server` deploy.**
- `ablo init` detects the `src/app` layout and writes the real sandbox key;
  `--help`/`-h` prints usage; `ablo dev --no-watch` exits after one push.
- Name the client with `typeof sync` (not `ReturnType<typeof Ablo>`).
</Update>

<Update label="June 15, 2026" description="v0.11.0">
## Canonical `claim` vocabulary + sync-group area of interest

### Breaking

- **`intent` → `claim` everywhere**: `useClaim` replaces `useIntent`, the
  `Ablo.Claim.*` namespace replaces `Ablo.Intent.*`, `Register` registers
  `Claims`. Wire frames moved `intent_*` → `claim_*` — clients and servers must
  run a `claim_*`-aware build together.

### Added

- **Sync-group area of interest** — read interest is no longer frozen at connect;
  `update_subscription` drives live re-indexing with `enterScope` / `leaveScope`
  / `pinScope` / `unpinScope`.
- **Richer claim-rejection errors** — rejections carry `heldByClaim` /
  `policyReason`, and `AbloClaimedError` exposes a typed `claims` array.
- Participant identity is canonical `user` | `agent` | `system`.
</Update>

<Update label="June 12, 2026" description="v0.10.1">
## Docs: 0.10.0 migration entry

Adds the `test`/`live` → `sandbox`/`production` environment rename (key prefixes
unchanged) and the `transport: 'http'` stateless client to the Version History &
Migration Guide.
</Update>

<Update label="June 12, 2026" description="v0.10.0">
## Stateless HTTP transport + canonical environment vocabulary

### Added

- **`Ablo({ transport: 'http' })`** returns a stateless client for agents,
  workers, and serverless — the same `ablo.<model>` surface with no WebSocket,
  one HTTP round-trip per call. The return type narrows so stateful-only APIs
  (`get`/`getAll`/`onChange`) are compile errors.
- **Source-mode commit scoping** — `commit` forwards `projectId`, `accountScope`,
  and `environment` to customer storage resolvers.

### Breaking

- Environment enum values are renamed to `production` / `sandbox` (the
  `*_live_` / `*_test_` key prefixes are preserved).
</Update>

<Update label="June 12, 2026" description="v0.9.15">
## Package metadata

Set the npm description to "The Collaboration Layer For AI Agents" so it stops
reverting to the old text on publish.
</Update>

<Update label="June 11, 2026" description="v0.9.14">
## README: real typed snippet

Replace the pseudo-diagram with a real typed snippet
(`create`/`retrieve`/`update`/`claim`) and tidy the Get-started line.
</Update>

<Update label="June 11, 2026" description="v0.9.13">
## Per-project axis

Schemas, planes, routing, and enforcement are scoped per project. Adds the
control plane, per-project key scoping with identity threading, a `remove_model`
gate, and the CLI/docs to drive it.
</Update>

<Update label="June 11, 2026" description="v0.9.12">
## README: point doc links at `docs.abloatai.com`

The Docs / Quickstart / API header links now point at `docs.abloatai.com`
instead of `abloatai.com`, which 307-redirects to the marketing site.
</Update>

<Update label="June 11, 2026" description="v0.9.11">
## `Model<'name'>` helper + `sessions.create` fixes

- **`Model<'tasks'>`** via the `Register` binding names your model in one
  parameter instead of restating `typeof schema`; `InferModel` is deprecated.
- Fix `sessions.create({ user })` 403 — user sessions now mint an `ek_` via the
  `sk_`-gated ephemeral-key door (was wrongly routed through `/auth/capability`).
- New: `ablo.organizationId` (after `ready()`), `ablo status --json`, typed
  `SyncGroupInput`.
</Update>

<Update label="June 11, 2026" description="v0.9.10">
## README: brand header

Add a centered brand header (banner, tagline, doc nav links, status badges).
</Update>

<Update label="June 11, 2026" description="v0.9.9">
## Docs: migration guide refinements

Version history & migration guide refinements plus changelog, audit, and link
fixes.
</Update>

<Update label="June 11, 2026" description="v0.9.8">
## Docs: Version History & Migration Guide

Add a Version History & Migration Guide, bring the changelog current, and sync
doc trees. Drop the dormant `causedByTaskId` from the audit-row docs and fix the
`ablo mode` argument vocabulary.
</Update>

<Update label="June 11, 2026" description="v0.9.7">
## Docs: correct `commits.create` operation shape

The advanced commit example now uses the public `{ action, model, data }`
operation shape that the SDK actually accepts (it previously showed `resource:`
and an internal wire form). No code changes.
</Update>

<Update label="June 11, 2026" description="v0.9.6">
## CLI quickstart down to three commands

`ablo init` now owns login, the standalone `migrate` step is dropped from the
`databaseUrl` quickstart (`dev` handles it), and the watch command is renamed
for honest naming. Also fixes three production bugs surfaced by the new
end-to-end journey test harness.

### Breaking

- **`ablo dev` is now `ablo push`.** Update any scripts that called `ablo dev`.
</Update>

<Update label="June 10, 2026" description="v0.9.5">
## Scoped-role automation and tenant-routing fix

`ablo migrate` now auto-creates the RLS-gated scoped role with zero SQL, using a
log-safe SCRAM-SHA-256 password verifier, plus a Neon/Supabase scoped-role
`databaseUrl` recipe. Fixes a JSONB double-encode that corrupted per-tenant
routing and silently fell back to the shared pool.
</Update>

<Update label="June 10, 2026" description="v0.9.4">
## Sync-position correctness and CLI hardening

Five scattered sync cursors are consolidated into one typed `syncPosition`
(persisted / applied / acked with a derived `readFloor`), fixing a claim taken
right after an ack-confirmed write reading stale against that write's own delta.
Adds transaction ack-confirmation, schema DDL-first push, and a reworked CLI.
</Update>

<Update label="June 9, 2026" description="v0.9.3">
## Onboarding leads with your own database

The quickstart now leads with the bring-your-own-database path (Drizzle Data
Source), drops the Ablo-managed mode, and adds the `ablo push` step.
</Update>

<Update label="June 9, 2026" description="v0.9.2">
## First-try setup for people and AI agents

Integrating Ablo now works on the first attempt: `ablo init` scaffolds a project
that builds and runs the current API, and the docs + error messages guide you to
the one step everything depends on — `ablo push`.

- **`ablo init` produces a working project** — browser provider + session route,
  current hooks and object-param verbs, and no `sk_` key in the browser bundle.
- **`ablo push` is self-documenting** — writing to a model the server hasn't seen
  fails with an error that tells you to run it. The server keeps its own copy of
  the schema, so this is the step that makes writes work.
- **Drizzle Data Source derives columns from your schema** — `drizzleDataSource(db, schema)`
  maps snake_case columns exactly as `ablo migrate` provisions them, so synced
  models need no parallel Drizzle table. Your auth and other non-synced tables
  stay in your own ORM schema: one database, two schemas, side by side.
- **`claim` is the one coordination API** — the internal `intents` accessor is
  deprecated; use `ablo.<model>.claim`.
- Requires **Node 24+**.

### Breaking

- Removed the `turn` primitive and the agent-work `tasks` resource. The client
  surface is now purely `ablo.<model>` + `claim`: `engine.beginTurn()`, the
  `Turn` type, `agent().run()`, and the agent/task type family are gone.
  Coordinate concurrent writers with `claim`; attribution comes from the
  server's auth context (`actor` / `onBehalfOf` / `capabilityId`).

</Update>

<Update label="June 5, 2026" description="v0.9.0">
## Single options object for every model verb

The model read and write verbs now take one options object instead of
positional arguments, so the id, the data, and every modifier live as named
siblings. `claim` returns a disposable handle instead of taking a callback.

### Breaking

- `create`, `update`, `delete`, and the async server `retrieve` each take a
  single options object: `create({ data, id? })`, `update({ id, data, ...options })`,
  `delete({ id, ...options })`, `retrieve({ id, ...options })`. Reactive local
  reads use `get(id)` (synchronous) — `useAblo((ablo) => ablo.tasks.get(id))`.

  ```diff
  - await ablo.tasks.update(id, { status: 'done' }, { wait: 'confirmed' })
  + await ablo.tasks.update({ id, data: { status: 'done' }, wait: 'confirmed' })

  - await ablo.tasks.retrieve(id)
  + await ablo.tasks.retrieve({ id })

  - useAblo((ablo) => ablo.tasks.retrieve(id)) ?? serverTask
  + useAblo((ablo) => ablo.tasks.get(id)) ?? serverTask
  ```

- `claim` returns a disposable handle instead of taking a callback. The handle
  exposes the fresh row on `.data` and is released on scope exit (`await using`)
  or explicitly via `.release()`. `claim.state`, `claim.queue`, `claim.release`,
  and `claim.reorder` also take the options object.

  ```diff
  - await ablo.tasks.claim(id, async (task) => {
  -   await ablo.tasks.update(task.id, { status: 'in_review' })
  - })
  + await using claim = await ablo.tasks.claim({ id })
  + const task = claim.data
  + await ablo.tasks.update({ id: task.id, data: { status: 'in_review' } })
  ```

</Update>

<Update label="June 2, 2026" description="v0.8.0">
## Callable `claim` namespace and direct Postgres connector

Coordination collapses into a single callable `claim` accessor, and a new
`databaseUrl` option lets you opt into the direct Postgres connector.

### Added

- Callable `claim` coordination namespace. `claim(id, work)` acquires a claim
  and runs `work` while it's held; `claim.state(id)`, `claim.queue(id)`,
  `claim.release(id)`, and `claim.reorder(id, order)` cover the rest of the
  lifecycle.
- `databaseUrl` option on `Ablo({ schema, apiKey, databaseUrl })` for the direct
  Postgres connector. Server-side only; defaults to `process.env.DATABASE_URL`.
  See the data-sources guide for setup and role requirements.

### Breaking

- Removed the flat coordination methods `claimState`, `queue`, `release`, and
  `reorder`. Use the `claim` namespace instead.

  ```diff
  - await ablo.task.claimState(id)
  - await ablo.task.release(id)
  + await ablo.task.claim.state(id)
  + await ablo.task.claim.release(id)
  ```

</Update>

<Update label="May 28, 2026" description="v0.7.0">
## Error contract, schema engine, and the `ablo` CLI

A versioned error contract across HTTP and WebSocket, a schema diff/migration
engine, and the `ablo` CLI for hosted, Data Source, and direct-connector
workflows.

### Added

- Closed error-code registry as the `code` field of the error model. HTTP and
  WebSocket errors share one envelope:
  `{ type, code, message, doc_url, request_id, ...details }`.
- `ERROR_CONTRACT_VERSION`, emitted in `errors.json` and on the `Ablo-Version`
  response header. `errors.mdx` and `errors.json` are generated from the
  registry, with a CI check that fails on drift.
- Request correlation: every response includes a `req_…` id (or the inbound
  `x-request-id`), also set as the envelope's `request_id`.
- Schema diff and migration planning via `generateProvisionPlan` and
  `generateMigrationPlan` (`@abloatai/ablo/schema`), with constant-value
  backfill for new required fields.
- `ablo generate` emits TypeScript types from the schema.
- CLI commands: `init`, `login`, `logout`, `status`, `mode`, `dev`, and `logs`.
  Authentication uses the OAuth 2.0 device flow; `login` provisions a test and a
  live key.
- Table-adoption commands run against your `DATABASE_URL`: `pull` generates
  `defineSchema(...)` from existing tables, `check` verifies them without changing your schema,
  and `migrate` applies changes for the direct connector. `schema push` and
  `dev` target the hosted sandbox, where the server diffs, migrates, and
  activates the schema.

### Breaking

- Removed the React hooks `useQuery`, `useOne`, `useMutate`, and `useReader`.
  Use `useAblo()` with `ablo.<model>.*`. The `MutateActions`, `ReaderActions`,
  and `ReaderFindOptions` types are still exported.

  ```diff
  - const { data } = useQuery('task', { where: { done: false } })
  + const ablo = useAblo()
  + const tasks = ablo.task.list({ where: { done: false } })
  ```

- Replaced the `{ error, reason }` OpenAPI error schema with the envelope and a
  generated `ErrorCode` enum.
</Update>

<Update label="May 26, 2026" description="v0.6.0">
## Coordination: fair queue, wait-line, and lease renewal

Claims acquire through a server-side FIFO queue. Reads are never gated by a
claim, including for agents.

### Added

- Claims acquire through a FIFO queue. On contention a claim waits its turn and
  re-reads before proceeding; a write blocked by another participant's claim
  throws `AbloBusyError`.
- `ablo.<model>.queue(id)` — reactive read of the wait-line behind a row: who is
  queued, their action, and FIFO position. Synced to peers like `activity(id)`.
- Backpressure on `claim`: `{ wait: false }` skips instead of waiting if the row
  is held; `{ maxQueueDepth: n }` throws `AbloBusyError('queue_too_deep')`
  rather than joining a line already that deep.
- Lease renewal: a held claim renews while the holder's connection is alive, so
  there is no TTL to size; it lapses only after the holder goes silent. An
  abandoned queued claim is dequeued.
- A waiting claim is an `Intent` with `status: 'queued'`; `position` carries its
  place in line.

### Breaking

- Change listeners renamed to `.onChange(...)`: `ablo.<model>.subscribe(cb)`,
  `presence.subscribe()`, `intents.subscribe()` → `.onChange(...)`.
- Row-access API renamed Resource → Model: `Ablo.Resource.*` → `Ablo.Model.*`,
  `ablo.resource(name)` → `ablo.model(name)`, `ModelTarget.resource` →
  `ModelTarget.model`, error code `resource_not_found` → `model_not_found`.
</Update>

<Update label="May 26, 2026" description="v0.5.1">
## Documentation

- Added a React quick-start (provider + `useAblo`), a plain-language rewrite, and
  a "Set up with Claude Code" section.
</Update>

<Update label="May 25, 2026" description="v0.5.0">
## Claim vocabulary and `bootstrapMode`

### Added

- `<AbloProvider>` gains a `bootstrapMode` prop (`'full' | 'none'`) to skip the
  baseline pull on read-light pages.
- `StaleContextConflict` gains an optional `conflictingFields`.

### Breaking

- Renamed the model intent handle methods (`ablo.<model>.intent(id)`):
  `acquire` → `claim`, `acquireOrAwait` → `claimOrWait`, `settled` → `whenFree`,
  `release` → `finish`, `revoke` → `cancel`. The lower-level `IntentHandle` /
  `IntentLeaseHandle` (`ablo.intents.*`) are unchanged.
</Update>

<Update label="May 25, 2026" description="v0.4.0">
## Intents

Coordinate writes to an entity with the same accessor you read it:
`ablo.<model>.intent(id)`.

```ts
const lock = ablo.documents.intent(doc.id);

lock.current;                              // who holds it (or null) — sync + reactive
lock.status;                              // 'idle' when free

await lock.claimOrWait();                // take it, or wait out the holder then take it
await lock.update({ status: 'done' });   // optimistic, lease-guarded
await lock.finish();
```

- `claimOrWait()` is the serialize-on-contention call — you never branch on who
  holds the target; you just get it safely. Bind it to an agent's write tool so
  agents never reason about coordination.
- Handles are `AsyncDisposable` — `await using lock = …` finishes on scope exit.

**Deprecated:** participant-level `intents.claim()` / `onRejected()` are superseded
by the per-model handle and will be removed in a future release.
</Update>

<Update label="April 22, 2026" description="v0.3.0">
  ## Umbrella `<AbloProvider>` for React

  One provider component now owns the full lifecycle — singleton rotation on auth
  change, Strict-Mode-safe bootstrap, `beforeunload` cleanup, session-expiry
  IndexedDB wipe, post-bootstrap hooks, mesh client construction. The reference
  integration shrank from 515 LOC of hand-rolled wiring to a 60-LOC wrapper.

  ### Added

  - `<AbloProvider>` — umbrella provider at `@abloatai/ablo/react` with declarative
    data, auth, behavior, and callback props.
  - `<SyncGroupProvider id="…">` + `useSyncGroup()` — per-entity scope context.
  - `<ClientSideSuspense fallback={…}>` — gate renders until the engine is `connected`.
  - `useSyncStatus()` rewritten as a tagged union — impossible states are unrepresentable.
  - `useCurrentUserId()`, `useErrorListener(cb)`.
  - Commit payload projection built into `TransactionQueue` — wire payloads derived
    from the schema alone; hand-rolled extractor tables can be deleted.

  ### Breaking

  - Removed `<SyncProvider>` (folded into `<AbloProvider>`) and `createAbloContext()`
    — mesh is always-on; `useAblo()` / `useParticipant()` always available.
  - Removed `withSync` (import `observer` from `mobx-react-lite` directly).
  - `useSyncStatus()` now returns a tagged union instead of six booleans:
    `const isReady = useSyncStatus().name === 'connected'`.

  No breaking change to `useQuery` / `useOne` / `useMutate` / `useReader` / `useMutators` /
  `useUndoScope` / `usePresence` / `useIntent`.
</Update>

<Update label="April 22, 2026" description="v0.2.1">
  ## React bindings hardening

  Fixes two infinite-loop classes that surfaced downstream as React error #185
  ("Maximum update depth exceeded"), and exposes sync-status reactivity as a
  first-class observable + hook.

  ### Fixed

  - `useQuery` / `useOne` no longer loop on `getSnapshot` — the snapshot is cached
    in a ref and refreshed only inside the subscribe callback.

  ### Added

  - `BaseSyncedStore` sync status is now properly observable (`isReady`, `isSyncing`,
    `isOffline`, `isReconnecting`, `isError`, `hasUnsyncedChanges`).
  - `useSyncStatus()` React hook — reactive snapshot bridged via `useSyncExternalStore`.

  No breaking changes.
</Update>

<Update label="April 21, 2026" description="v0.2.0">
  ## Mesh SDK — the canonical agent-multiplayer surface

  The SDK covers exactly three integration shapes, each with a canonical example:
  **server agent**, **browser app** (scoped capability token, Stripe `client_secret`
  shape), and **sub-agent** (attenuated from the parent capability).

  ### Ergonomics

  - `Ablo` class — `import Ablo from '@abloatai/ablo'` / `new Ablo({ schema })`,
    matching `new Stripe()` / `new OpenAI()`.
  - Model-scoped joins — `ablo.matters.join(id, { label })`.
  - `as` alias replaces `onBehalfOf`; duration strings (`ttl: '3m'`); auto-connect.

  ### Coordination primitives

  - Presence verbs — `presence.editing(target)` / `viewing(target)` / `idle()`.
  - Intent verbs — `intents.editing(target)` / `writing(target)`, returning an
    `IntentHandle` with `Symbol.asyncDispose` for `await using`.
  - Snapshots and async iterables over presence, intents, and deltas.

  ### Config

  - `organizationId` no longer required — the API key or session binds the caller's org.
  - `createMeshFromEnv` removed — `new Ablo({ schema })` auto-reads env.
</Update>

<Update label="April 10, 2026" description="v0.1.0">
  ## Initial release

  - **Schema DSL** — zero-codegen schema with full TypeScript inference.
  - **React hooks** — reactive data binding for models, mutations, and queries.
  - **Consumer API** — `createSyncEngine()` one-liner setup.
  - **Offline-first** — IndexedDB persistence with an FK-safe offline mutation queue.
  - **Real-time sync** — WebSocket delta streaming with optimistic updates and rollback.
  - **AI Agent SDK** — `SyncAgent` for agents as first-class sync citizens.
  - **Pluggable auth** — API key, JWT, and session providers.
</Update>
