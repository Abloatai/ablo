/**
 * @ablo/sync-engine-internal/core
 *
 * First-party runtime internals. Public integrations should import
 * `Ablo` from `@ablo/sync-engine` and use Resource -> Intent -> Commit.
 */
export * from '../core/index.js';
