/**
 * Internal schema adapter shim
 *
 * First-party schema adapter for local model metadata and server
 * mutator metadata. Not required for the API-key state API.
 */
export * from '../schema/index.js';
