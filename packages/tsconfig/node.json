{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Node — for Node-runtime apps and packages (sync-server, agent-worker, scripts).",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "types": ["node"]
  }
}
