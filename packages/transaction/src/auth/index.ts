/**
 * Authentication subsystem.
 *
 * Start here and descend into credential selection, capability contracts,
 * session issuance, identity resolution, endpoint protocol, or hosted routing.
 */
export * from './runtime.js';
